require 'rspec'
require 'capistrano'
require 'cap-elb'

describe Cap, "baseline" do
	it "returns a good error if no load balancer found" do
		true.should == true
	end
	it "returns a good error if no load balancer found" do
		true.should == true
	end
	it "returns a good error if no load balancer found" do
		true.should == true
	end
end
